<?php

/**
 * Implements template_preprocess_html().
 */
function general_config_preprocess_html(&$variables) {
  // Checking device to do respective redirects.
  $detect = mobile_detect_get_object();
  if ($detect->isMobile()) {
//    drupal_goto();
  }
}
